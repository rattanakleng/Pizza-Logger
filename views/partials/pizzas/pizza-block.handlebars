<li class="text-dark h5">
	<div class="row">
		<div class="col-lg-6">
			{{name}}
		</div>
		<div class="col-lg-6">
			<div class="row justify-content-end">
				<button class="change-devoured btn btn-secondary" data-id="{{id}}" data-newdevoured="{{devoured}}">
					{{#if devoured}}Devour!{{else}}Add to menu!{{/if}}
				</button>

				<button class="delete-pizza btn btn-secondary ml-3" data-id="{{this.id}}">Delete!</button>

			</div>

		</div>
	</div>
</li>